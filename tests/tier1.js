const {expect} = require('chai')

const three = 3
const five = 5

describe('Math', () => {
  describe('Add 1 + 1', () => {
    expect(three + five).to.be.be(10)
  })
})

// describe('Cart API', function () {
//   describe('Add Product to Cart', function () {
//     it('addProduct post route should add a product to the Order_Product through table', function () {
//       expect
//     })
//   })
// })
